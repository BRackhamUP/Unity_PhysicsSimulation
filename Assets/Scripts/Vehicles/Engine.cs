using UnityEngine;

public class Engine : MonoBehaviour
{
    public float torque;
    public float rpm;
    public float maxRPM;

    public void ApplyThrottle(float throttle, float deltaTime)
    {
        // increase RPM based on throttle
    }

}
